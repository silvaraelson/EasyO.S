# node-modules "achatado" (sem os symlinks do virtual store .pnpm/) — necessário
# porque o Gradle/prefab do Android não resolve esses symlinks corretamente no
# Windows, quebrando o build nativo do app mobile (Expo/React Native).
# https://github.com/expo/expo/issues/40963
node-linker=hoisted
